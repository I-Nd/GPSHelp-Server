server:
    port: 8088

spring:
    datasource:
        url: jdbc:mysql://144.34.220.135:3306/wechat_gpshelp?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=UTC
        username: root
        password: 123456
         # 使用阿里巴巴的开源框架druid数据库连接池
        type: com.alibaba.druid.pool.DruidDataSource
        platform: mysql
        # mysql 8.0.+使用的驱动
        driver-class-name: com.mysql.cj.jdbc.Driver



    jackson:

        default-property-inclusion: non_null
        serialization:
            indent-output: true
        date-format: yyyy-MM-dd HH:mm:ss
        parser:
            allow-missing-values: true
    mvc:
        static-path-pattern: /**


mybatis:
      # mapper映射文件地址，这里我在src/main/resouces下新建文件夹mapper
     # mapper-locations: classpath*:mapper/*.xml
     # 配置bean扫描目录，也可用注解
     type-aliases-package: classpath*:com.example.study.dataobject


